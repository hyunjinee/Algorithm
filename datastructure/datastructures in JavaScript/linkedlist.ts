// A linked list is a linear data structure where the elements are not stored in contiguous memory locations. the elements in a linked list are linked using pointers

// Linked list uses pointer

// Doubly Linked List 
// has pointers to its previous node .we can traverse backwards on a doubly linked list